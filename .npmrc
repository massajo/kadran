# Les dependances de build doivent etre approuvees explicitement (pnpm >= 10).
# Une dependance qui execute un script d'installation est une surface d'attaque :
# on la declare, on ne l'autorise pas par defaut.
strict-peer-dependencies=false
auto-install-peers=true
